import{NextRequest,NextResponse}from"next/server";import{isValidPlate,normalizePlate}from"@/lib/validation/plate";import{fetchAllRDWData}from"@/lib/rdw/client";import{transformRDWData}from"@/lib/rdw/transformer";const store=new Map();export async function GET(req,{params}){const{plate:raw}=await params;const plate=normalizePlate(raw||"?");if(!isValidPlate(plate))return NextResponse.json({success:false,error:"INVALID_PLATE",message:"Ongeldig kenteken."},  {status:400});let r;try{r=await fetchAllRDWData(plate);}catch(e){return NextResponse.json({success:false,error:"UPSTREAM_ERROR",message:"RDW niet bereikbaar."},{status:503});}if(r.upstreamError)return NextResponse.json({success:false,error:"UPSTREAM_ERROR",message:"RDW storing."},{status:503});if(!r.vehicleBase)return NextResponse.json({success:false,error:"NOT_FOUND",message:"Kenteken "+raw.toUpperCase()+" niet gevonden."},{status:404});try{const data=transformRDWData(plate,r.vehicleBase,r.apkData,r.fuelData);return NextResponse.json({success:true,data},{status:200});}catch(e){return NextResponse.json({success:false,error:"INTERNAL_ERROR",message:"Interne fout."},{status:500});}}